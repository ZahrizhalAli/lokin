[project]
name = "lokin"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "fastapi>=0.129.0",
    "pipecat-ai",
]

[tool.uv.sources]
pipecat-ai = { path = "../pipecat" }
